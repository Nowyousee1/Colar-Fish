# 硬件接线说明 — 自动化流程控制系统

## 1. 引脚分配总表（2026-09-05 版）

| 设备 | 信号 | 引脚 | 复用功能 | 说明 |
|------|------|------|----------|------|
| 日本电机 | PWM 输入 | **PC9** | TIM8_CH4 (AF3) | 1kHz PWM，100%=转 / 0%=停 |
| K15 继电器 | 控制线 | **PD7** | GPIO 输出 | 高电平吸合，正转时序窗口 |
| K16 继电器 | 控制线 | **PD4** | GPIO 输出 | 高电平吸合，反转时序窗口 |
| 步进电机 (TB6600) | PUL+ | **PB8** | TIM4_CH3 (AF2) | 6400Hz 50% 方波脉冲 |
| 步进电机 (TB6600) | DIR+ | **PB9** | GPIO 推挽输出 | 低=CW，高=CCW |
| 水泵（继电器） | 控制 | **PG5** | GPIO 输出 | 高电平 = 开 |
| 电磁阀（继电器） | 控制 | **PG3** | GPIO 输出 | 高电平 = 开 |
| 调试串口 USART1 | TX/RX | **PA9 / PA10** | AF7 | 115200-8-N-1 |
| 指示灯 LED0 | — | **PF9** | GPIO 输出 | 流程步骤指示 |

### 已删除/释放的引脚（不要再接）

| 引脚 | 原用途 | 状态 |
|------|--------|------|
| PB1 | 原 K1 位置继电器（气泵） | 2026-09-05 代码删除，引脚空闲 |
| PB3 / PB4 / PB5 | L298N 旧接法（IN2/IN1/ENA） | 2026-09-03 弃用（实测输出异常 ~1V） |
| PB14 / PB15 / PB10 | L298N 新接法（IN1/IN2/ENA） | 2026-09-05 随 L298N 方案删除，引脚空闲 |

### 引脚占用守则（新增外设前必读）

- **PB9 已被步进电机 DIR+ 占用**，不可再分配。
- **PD4 / PD7 本工程新启用**：注意探索者开发板上这两个脚接 FSMC（PD4=NOE、PD7=NE1，LCD 坐标线），
  若板子外接 LCD 会冲突；不接 LCD 则可正常使用。
- PA13/PA14 为 SWD 调试口，不可占用。
- PB3/PB4 为 F4 的 JTAG 复用脚（JTDO/NJTRST），如需启用为 GPIO，直接配 MODER 输出即可（F4 无需 AFIO 重映射）。

## 2. K15 / K16 继电器接线（替代原 L298N 直流电机方案）

| 继电器板 | 接到 | 说明 |
|----------|------|------|
| K15 控制端子 | PD7 | 高电平吸合（与 PG5/PG3 继电器模块同类型） |
| K16 控制端子 | PD4 | 高电平吸合 |
| 继电器板 GND | STM32 GND | **必须共地** |
| 继电器板 VCC | 5V 电源 | 按继电器模块要求 |
| K15/K16 触点 | 直流电机电源回路（按实际用途接） | 若用于电机换向，注意两路互锁——**K15、K16 绝不可同时吸合** |

时序对应关系（与原 L298N 引脚时序完全一致）：

| 时序窗口 | 原引脚 | 现继电器 | 时长 |
|----------|--------|----------|------|
| Step 2（原正转窗口） | PB4 (IN1) | **K15 (PD7)** | 2s |
| Step 3-1（原反转窗口） | PB3 (IN2) | **K16 (PD4)** | 2s |
| 其余时间 | — | 均断开 | — |

## 3. 步进电机 TB6600 接线（共阳）

| TB6600 | 接到 | 备注 |
|--------|------|------|
| PUL+ | PB8 | 6400Hz 脉冲（200 脉冲/转、1.8° 步距角时约 1920 RPM） |
| PUL− | GND | 共阳接法 |
| DIR+ | PB9 | 低=CW，高=CCW，换向至少提前脉冲 5µs 建立 |
| DIR− | GND | 共阳接法 |
| ENA+/− | 悬空 | 未使用 |
| A+/A−/B+/B− | 电机绕组 | 按电机铭牌相序 |

注意：3.3V 驱动 TB6600 光耦电流约 7.8mA，处于推荐范围（5~16mA）下限，功能可用但偏小；
若出现丢步可考虑 5V 串 1kΩ 电阻驱动。

## 4. 继电器直驱设备

| 设备 | 引脚 | 说明 |
|------|------|------|
| 水泵 | PG5 | 高电平吸合 |
| 电磁阀 | PG3 | 高电平吸合，Step 5 中第 4s 通、第 8s 断（原气泵联动已删除） |

## 5. 上电与测量注意事项

1. **上电瞬间引脚浮空**：从复位到 `system_init()` 完成前各输出脚为浮空态，继电器可能有
   瞬间微动作。若在意，可在继电器控制脚对地加 10kΩ 下拉。
2. **万用表测量时机**：K15 在 Step 2（2s）、K16 在 Step 3-1（2s）吸合，其余时间 PD7/PD4 均为 0V，属正常设计。
   一个周期（约 31s）内 K15/K16 各只动作一次。
3. **期望值**（串口打印对应 Step 的窗口内，黑表笔接 GND）：
   - Step 2 窗口：PD7 ≈ 3.3V，PD4 = 0V
   - Step 3-1 窗口：PD4 ≈ 3.3V，PD7 = 0V
4. **必须共地**：STM32、继电器板、TB6600、电源之间的 GND 全部连通，
   否则信号无效且万用表读数不可信。
5. **验证固件在跑**：先看串口是否打印 banner 与 Step 日志（终端编码 GBK/ANSI），再测引脚电压，避免误判。
