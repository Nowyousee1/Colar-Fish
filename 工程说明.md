# 工程说明 — 自动化流程控制系统

## 1. 项目概述

本项目基于 STM32F407ZGT6（正点原子探索者 V3 开发板）实现一套多设备自动化时序控制：
按固定周期循环驱动 **日本电机（PWM）、K15/K16 继电器（原直流电机正反转时序）、步进电机（TB6600）、水泵、电磁阀**，
完成一个约 29 秒的工艺流程，无限循环。

- MCU：STM32F407ZGT6，LQFP144，主频 168MHz
- 工具链：Keil MDK 5.24 + ARMCC V5.06 update 5
- 代码风格：**寄存器直接操作**（仅使用 CMSIS 设备头文件 `stm32f407xx.h`，未使用 HAL/标准库外设驱动）
- 源文件编码：**GBK/GB2312**（编辑中文后请保持 GBK 编码，否则 Keil 编译告警、串口乱码）

历史沿革：直流电机最初由继电器（RELAY_2/PC7，威恒）控制 → 改为 L298N 驱动（先后用 PB3/PB4/PB5、PB14/PB15/PB10）→ 2026-09-05 改回**双继电器方案**（K15/PD7、K16/PD4），L298N 相关代码全部删除；同时原 K1 位置继电器（RELAY_5/PB1，气泵）代码删除。

## 2. 时钟系统

| 项目 | 配置 |
|------|------|
| 时钟源 | HSE 8MHz 外部晶振 |
| PLL | M=8, N=336, P=2, Q=7 |
| SYSCLK / AHB | 168MHz |
| APB1 | 42MHz（定时器时钟 ×2 = 84MHz） |
| APB2 | 84MHz（定时器时钟 ×2 = 168MHz） |
| Flash | 5 等待周期 + 预取 + I-Cache + D-Cache |

时钟初始化在 `main.c` 的 `clock_init()` 中完成（寄存器方式）。

## 3. 软件架构

```
├── USER
│   ├── main.c            流程控制主循环 + 时钟初始化 clock_init()
│   ├── main.h            公共宏（GPIO 模式/引脚掩码/PLL 参数）+ 统一包含各模块头文件
│   └── stm32f4xx_it.c    内核异常处理（SysTick/USART1 中断分别在 delay.c / usart.c 内实现）
├── SYSTEM
│   ├── delay/delay.c     SysTick 延时（delay_init(168), delay_ms/delay_us）
│   ├── usart/usart.c     USART1 串口驱动 + printf 重定向（115200）
│   └── sys/sys.c         系统辅助
├── HARDWARE
│   ├── pwm/pwm.c         单路 PWM 框架：CH1(PC9, TIM8_CH4)
│   ├── motor/motor.c     日本电机封装（PWM_CH1：全速/停/调速），首次调用时惰性初始化 PWM
│   ├── relay/relay.c     继电器：RELAY_1(日本电机, PWM 路径) + RELAY_3/4 + RELAY_K15/K16 GPIO 直驱
│   └── stepper/stepper.c TB6600 步进电机（TIM4_CH3 脉冲 + PB9 方向）
└── CMSIS / CORE          CMSIS 头文件与启动文件 startup_stm32f407xx.s
```

注：原 `HARDWARE/dc_motor`（L298N 直流电机驱动）模块已于 2026-09-05 整体删除。

初始化顺序（`system_init()`）：

```
clock_init() → delay_init(168) → usart_init(84, 115200)
→ relay_gpio_init(水泵/电磁阀/K15/K16)       ← 先确立 GPIO 低电平，防止继电器误闪
→ relay_init(RELAY_1) → motor_init() → pwm_init()   ← TIM8 PWM 初始化
→ Stepper_Init() + Stepper_SetSpeed(6400Hz)
```

## 4. 自动化流程（主循环，无限周期）

| 顺序 | 动作 | 时长 |
|------|------|------|
| Step 1 | 日本电机 ON | 3s |
| — | 间隔 | 0.5s |
| Step 2 | **K15 继电器 ON**（原直流电机正转时序）+ 步进电机 CW | 2s |
| — | 间隔 | 0.5s |
| Step 3-1 | 日本电机 ON + **K16 继电器 ON**（原直流电机反转时序） | 2s |
| — | 间隔 | 0.5s |
| Step 3-2 | 步进电机 CCW | 2s |
| — | 间隔 | 0.5s |
| Step 3-3 | 日本电机 ON | 3s |
| — | 间隔 | 0.5s |
| Step 4 | 水泵 ON | 3s |
| — | 间隔 | 0.5s |
| — | [间隔] 日本电机 ON | 3s |
| — | 间隔 | 0.5s |
| Step 5 | （原气泵 K1/PB1 路已删除，仅保留 4s 延时骨架）电磁阀第 4s ON、第 8s OFF | 8s |
| — | 间隔 | 0.5s |
| — | [间隔] 日本电机 ON | 2s |
| — | 周期间隔，随后进入下一周期 | 2s |

PF9（LED0）在每个步骤执行期间翻转，作为流程运行指示。

## 5. PWM 资源分配

| 定时器 | 通道 | 引脚/复用 | 时钟 | PSC/ARR | 频率 | 用途 |
|--------|------|-----------|------|---------|------|------|
| TIM8（高级） | CH4 | PC9 / AF3 | 168MHz | 167 / 999 | 1kHz | 日本电机（100% 全速或 0%） |
| TIM4（通用） | CH3 | PB8 / AF2 | 84MHz | 83 / 动态ARR | 6400Hz | 步进脉冲（50% 占空比方波） |

说明：
- TIM8 为高级定时器，`pwm_init()` 中已置位 `BDTR.MOE` 使能主输出。
- 原 TIM2/PB10 通道（L298N ENA）已随 L298N 方案删除。

## 6. 构建、烧录与调试

### 构建
- Keil 打开 `Motor_Control.uvprojx`，**Project → Rebuild all target files**（改代码后建议全量重建）。
- 命令行验证：`D:/Keil5/UV4/UV4.exe -r Motor_Control.uvprojx -j0 -o build_claude.log`
- 注意：工程根目录下的 `build.log` 是历史遗留旧文件，判断实际构建状态请看 `Output/Motor_Control.build_log.htm` 的生成时间。
- **编码警告**：源文件为 GBK 编码，编辑器保存时务必保持 GBK/ANSI，不要存成 UTF-8。

### 烧录
- ST-Link + SWD（PA13/PA14），Keil 内 Download 即可。

### 串口调试
- USART1：PA9(TX) / PA10(RX)，115200-8-N-1，终端设为 GBK/ANSI 显示。
- 上电打印固件标识 banner（含 `PD7 -> K15继电器` / `PD4 -> K16继电器` 行）与每个 Step 的执行日志，
  可用于**确认板子上运行的是当前固件**。
